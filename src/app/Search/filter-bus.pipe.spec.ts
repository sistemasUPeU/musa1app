import { FilterBusPipe } from './filter-bus.pipe';

describe('FilterBusPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBusPipe();
    expect(pipe).toBeTruthy();
  });
});
