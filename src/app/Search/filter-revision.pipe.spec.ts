import { FilterRevisionPipe } from './filter-revision.pipe';

describe('FilterRevisionPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRevisionPipe();
    expect(pipe).toBeTruthy();
  });
});
