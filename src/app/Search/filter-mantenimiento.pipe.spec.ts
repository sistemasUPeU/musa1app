import { FilterMantenimientoPipe } from './filter-mantenimiento.pipe';

describe('FilterMantenimientoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMantenimientoPipe();
    expect(pipe).toBeTruthy();
  });
});
