import { FilterasignarconductorPipe } from './filterasignarconductor.pipe';

describe('FilterasignarconductorPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterasignarconductorPipe();
    expect(pipe).toBeTruthy();
  });
});
